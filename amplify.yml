version: 1
frontend:
  phases:
    preBuild:
      commands:
        - mkdir -p ~/.ssh
        - ssh-keyscan github.com >> ~/.ssh/known_hosts
        - npm install -g pnpm
        - pnpm install --frozen-lockfile
        - npx prisma generate
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths: 
      - .next/cache/**/*