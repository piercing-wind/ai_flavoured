/** @type {import('next').NextConfig} */
const nextConfig = {
  // webpack: (config, options) => {
  //   config.plugins.push(
  //     new options.webpack.IgnorePlugin({ resourceRegExp: /pg-native/ })
  //   );
  //   config.resolve.fallback = { ...config.resolve.fallback, fs: false,dns: false,net: false,tls: false,child_process: false, };

  //   return config;
  // },
};

export default nextConfig;
